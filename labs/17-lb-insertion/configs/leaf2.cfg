! Session 17 (leaf2) - UNTESTED. Base: chain 01->04.
! web1/web2/lb all in VLAN 20 (10.100.20.0/24). eth4/5 join VLAN 20:
interface Ethernet1/4
  switchport
  switchport mode access
  switchport access vlan 20
  mtu 9216
  no shutdown
interface Ethernet1/5
  switchport
  switchport mode access
  switchport access vlan 20
  mtu 9216
  no shutdown
! VIP route: the service address lives ON the lb (one-armed):
vrf context Tenant-A
  ip route 10.100.99.99/32 10.100.20.50
